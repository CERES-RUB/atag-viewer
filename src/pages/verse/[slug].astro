---
import Layout from '@layouts/Layout.astro';
import { VerseView } from './_components/VerseView';

export function getStaticPaths() {
  return [
    {params: {slug: 'canto-5'}}
  ];
}

const { slug } = Astro.params;
---
<Layout title="Verse">			
	<VerseView
		client:only="react" 
    slug={slug} />
</Layout>